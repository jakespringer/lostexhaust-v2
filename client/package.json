{
  "name": "lostexhaust",
  "private": true,
  "version": "0.0.0",
  "description": "A carpool application, done right",
  "repository": "https://www.lostexhaust.org",
  "license": "LGPL-3",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",
  }
}
